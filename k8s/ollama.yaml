apiVersion: apps/v1
kind: Deployment
metadata:
  name: ollama
  namespace: monadic-pipeline
  labels:
    app: ollama
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ollama
  template:
    metadata:
      labels:
        app: ollama
    spec:
      containers:
      - name: ollama
        image: ollama/ollama:0.6.2
        ports:
        - containerPort: 11434
          name: http
        resources:
          requests:
            memory: "4Gi"
            cpu: "2000m"
          limits:
            memory: "8Gi"
            cpu: "4000m"
        volumeMounts:
        - name: ollama-data
          mountPath: /root/.ollama
        livenessProbe:
          httpGet:
            path: /api/tags
            port: 11434
          initialDelaySeconds: 60
          periodSeconds: 30
          timeoutSeconds: 10
        readinessProbe:
          httpGet:
            path: /api/tags
            port: 11434
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
      volumes:
      - name: ollama-data
        persistentVolumeClaim:
          claimName: ollama-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: ollama-service
  namespace: monadic-pipeline
  labels:
    app: ollama
spec:
  selector:
    app: ollama
  ports:
  - protocol: TCP
    port: 11434
    targetPort: 11434
    name: http
  type: ClusterIP
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ollama-pvc
  namespace: monadic-pipeline
spec:
  accessModes:
    - ReadWriteOnce
  # Storage class options:
  # - managed-csi (Azure Disk CSI)
  # - ionos-enterprise-ssd (IONOS Cloud SSD - recommended for models)
  # - ionos-enterprise-hdd (IONOS Cloud HDD)
  # - gp2 or gp3 (AWS EBS)
  # - standard or premium-rwo (GCP)
  storageClassName: managed-csi
  resources:
    requests:
      storage: 20Gi
