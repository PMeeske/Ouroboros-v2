apiVersion: v1
kind: Secret
metadata:
  name: monadic-pipeline-secrets
  namespace: monadic-pipeline
type: Opaque
stringData:
  # ⚠️ SECURITY WARNING: Replace these placeholder values with actual secrets
  # 
  # RECOMMENDED APPROACH:
  # 1. Use external secret management (Azure Key Vault, HashiCorp Vault, AWS Secrets Manager)
  # 2. Or create secrets via kubectl command (see below)
  # 3. Never commit actual secrets to git
  # 
  # CREATE SECRETS VIA KUBECTL:
  # kubectl create secret generic monadic-pipeline-secrets \
  #   --from-literal=openai-api-key=YOUR_ACTUAL_KEY \
  #   --from-literal=vector-store-connection-string=http://qdrant-service:6333 \
  #   --from-literal=application-insights-connection-string=YOUR_ACTUAL_CONNECTION_STRING \
  #   --namespace=monadic-pipeline
  #
  # For development/testing only - replace with actual values:
  openai-api-key: "PLACEHOLDER-REPLACE-ME"
  vector-store-connection-string: "http://qdrant-service:6333"
  application-insights-connection-string: "PLACEHOLDER-REPLACE-ME"
---
# IONOS Container Registry Pull Secret
# Uncomment and configure if using IONOS private registry
# 
# CREATE VIA KUBECTL:
# kubectl create secret docker-registry ionos-registry-secret \
#   --docker-server=adaptive-systems.cr.de-fra.ionos.com \
#   --docker-username=<your-username> \
#   --docker-password=<your-password> \
#   --namespace=monadic-pipeline
#
# apiVersion: v1
# kind: Secret
# metadata:
#   name: ionos-registry-secret
#   namespace: monadic-pipeline
# type: kubernetes.io/dockerconfigjson
# data:
#   .dockerconfigjson: <base64-encoded-docker-config>
